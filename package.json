{
  "name": "seed",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "watchify grid/app.js -o grid/bundle.js"
  },
  "author": "Kevin Barabash (kevinb7@gmail.com)",
  "license": "MIT",
  "dependencies": {
    "babel": "^4.7.16",
    "rx": "^2.4.3"
  },
  "devDependencies": {
    "babelify": "^5.0.4"
  },
  "browserify": {
    "transform": [
      "babelify"
    ],
    "options": {
      "browserifyOptions": {
        "debug": true
      }
    }
  }
}
